name: Build and push Docker image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_PASSWORD }}
    - name: Build and Push Docker image
      uses: docker/build-push-action@v4
      with:
        push: true
        tags: sunwupark/app:latest
    
  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}

      - name: Pull image from Docker Hub
        run: docker pull sunwupark/app:latest

      - name: Run Docker container with environment variables
        run: docker run --name gocontainer -d -p 8080:80 sunwupark/app:latest

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: AKIASTBWTVPJLRJBBLPK
          aws-secret-access-key: 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
          aws-region: us-east-1

      - name: Setup npm package
        run: npm init -y && npm install && yarn install

      - name: Deploy to ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          task-definition: suntest-revision1.json
          cluster: suntest
          service: gogo2
          image: sunwupark/app:latest